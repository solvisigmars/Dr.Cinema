import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    marginBottom: 24
  },
  
  cinemaName: {
    fontSize: 22,
    fontWeight: "700",
    marginHorizontal: 16,
    marginBottom: 8
  }
});