import { StyleSheet } from "react-native";

export default StyleSheet.create({
  row: {
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 16
  },

  dragHandle: {
    fontSize: 26,
    color: "#888",
    paddingHorizontal: 10
  },

  removeButton: {
    fontSize: 22,
    color: "#EF4444",
    paddingHorizontal: 10
  }
});
